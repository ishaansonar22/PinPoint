# Shared secret. PinPoint sends it as "Authorization: Bearer <token>".
COMPILE_SERVICE_TOKEN=change-me

# Optional
PORT=8080
# ARDUINO_CLI=/usr/local/bin/arduino-cli
# COMPILE_TIMEOUT_MS=60000
# COMPILE_MAX_CONCURRENT=2
